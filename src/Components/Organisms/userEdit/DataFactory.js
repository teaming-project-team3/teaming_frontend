export function dataFactory(stats, data) {

    const newData = {
        "nickname": stats.userId.nickname,
        "introduction": stats.introduction,
        "profileUrl": stats.profileUrl,
        "position": stats.position,
        "front" : stats.front,
        "back" : stats.back,
        "design" : stats.design,
        "portfolioUrl" : stats.portfolioUrl,
        "url": stats.url,
        }
        

}

